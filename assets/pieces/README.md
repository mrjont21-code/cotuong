# Pieces directory

Quân cờ được vẽ trực tiếp bằng Canvas trong `js/game.js` (hàm `drawPiece`) dùng ký tự Unicode tiếng Trung.

Thư mục này dành cho tùy chỉnh sau này nếu muốn dùng ảnh/SVG thay vì vẽ Canvas.
